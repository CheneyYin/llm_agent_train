# 演示脚本 04：Agent 入门

> **运行环境**：Windows（CMD 或 PowerShell），命令已适配 Windows。Linux/macOS 用户将 `python` 换为 `python3`，`type` 换为 `cat`，`dir /s` 换为 `ls -R`。

## 场景准备

```bash
cd demo-project
```

**重要**：端到端演示（步骤 3）需提前执行一次并保存 session 日志，现场以回放 + 讲解方式呈现。以下命令在备课时执行：

```bash
# 备课时：提前运行一次完整的 Agent 任务，保存日志
# Linux / macOS:
# pi -p "分析 parse.py 和 sample.log，找出 parse.py 的 bug 并修复，修复后运行 python parse.py sample.log 验证输出为 6 条" \
#    --session-id demo-agent-001 2>&1 | tee /tmp/agent-demo-session.log
#
# Windows (PowerShell):
pi -p "分析 parse.py 和 sample.log，找出 parse.py 的 bug 并修复，修复后运行 python parse.py sample.log 验证输出为 6 条" --session-id demo-agent-001 2>&1 | Tee-Object -FilePath $env:TEMP\agent-demo-session.log
```

现场演示时讲师打开保存的 session 日志（Linux: `/tmp/agent-demo-session.log`，Windows: `%TEMP%\agent-demo-session.log`），滚动讲解关键步骤。若实时执行够快（< 2 分钟），可直接实时演示。

---

### 步骤 1：什么是 Agent — 概念演示

**前情过渡**：
"回顾一下我们现在做的事：写好提示词 → 看 AI 输出 → 判断结果行不行 → 不行就改提示词再来 → 再看输出……每一步都是你手动操作。有没有一种东西，能自动完成这个循环？它自己思考、自己调用工具、自己看结果、自己决定下一步？这个东西就叫 Agent。"

**讲师话术**：
"Agent 的本质就一句话：把'思考→行动→观察→再思考'的循环自动化。"

展示 Mermaid 图（取自讲义）：

"左边的图是我们刚才的 ReAct 演示——每一步都要你手动发指令。右边的图是 Agent——它自己跑整个循环。代码是一样的代码，工具是一样的工具，区别只是谁在按'下一步'按钮。"

**关键展示点**：
- Agent 不是新技术，它就是 ReAct 的自动化版本
- 你从"操作者"变成"监督者"

**衔出过渡**：
"Agent 听起来神秘，但拆开看核心就是一个简单的四步循环——和我们刚才手动做的 ReAct 几乎一模一样，只是现在 AI 自己来驱动这个循环了。"

---

### 步骤 2：Agent 核心循环拆解

**前情过渡**：
"Agent 听起来神秘，但拆开看核心就是一个简单的四步循环。"

**讲师话术**：
"我们看一下 pi agent 在执行一个任务时后台发生了什么。下面是它处理'分析 sample.log 并修复 parse.py'这个任务时的执行日志。"

**回放日志关键片段**（讲师逐段讲解）：

```
[Agent 收到任务] "分析 parse.py 和 sample.log，找出 bug 并修复"

--- 第1轮 ---
[思考] 我需要先了解数据。sample.log 是日志文件，先看看内容。
[行动] read_file("sample.log")
[观察] 6行日志：4×ERROR, 1×WARN, 1×INFO

--- 第2轮 ---
[思考] 日志有 WARN 和 INFO 级别。我需要检查 parse.py 是否正确处理所有级别。
[行动] read_file("parse.py")
[观察] parse_log 函数第9行正则：r'... ERROR ...' ——只匹配 ERROR

--- 第3轮 ---
[思考] 找到 bug：正则在 ERROR 处硬编码，漏掉 WARN/INFO。修复方案：用 \w+ 匹配任意级别。
[行动] edit("parse.py", line=9, old="ERROR", new="\w+")  # 实际更复杂
[观察] 修改完成

--- 第4轮 ---
[思考] 需要验证修复效果
[行动] run("python parse.py sample.log")
[观察] 输出 6 条记录 ✓ 修复成功

--- [循环结束] ---
[Agent 报告] "parse.py 的 bug 已修复。根因：第9行正则只匹配 ERROR 导致 WARN/INFO 被忽略。
修复：将 ERROR 改为 \w+ 匹配所有日志级别。验证：运行 parse.py sample.log 输出 6 条，与日志行数一致。"
```

**讲师在每轮停顿，指出现场对应的步骤**：

| 轮次 | 步骤 | 讲师点评 |
|------|------|---------|
| 第1轮 | 理解→行动→观察 | "它自己决定先看数据，不是我们告诉它的" |
| 第2轮 | 理解→行动→观察 | "它对比了日志和代码，发现不匹配——就像你做 code review" |
| 第3轮 | 理解→行动→观察 | "找到 bug，自动修复——注意它没有问'要不要修'，因为它理解任务目标是修复" |
| 第4轮 | 理解→行动→观察 | "自动验证——程序员的好习惯，Agent 也有" |

**关键展示点**：
- 每轮都是"理解→行动→观察"的循环
- Agent 自己决定每一步用什么工具、读什么文件
- 不需要你写 CoT 提示词——Agent 内部就是这么工作的

**衔出过渡**：
"理论讲完了，我们来看一个真实的例子——用 pi agent 从头到尾完成一个开发任务。注意观察：Agent 在每一步是怎么思考、怎么选工具、怎么根据结果调整的。"

---

### 步骤 3：端到端演示

**前情过渡**：
"理论讲完了，我们来看一个真实的例子。"

**讲师话术**：
"现在我把整个任务一句话丢给 pi agent，不做任何分步指导。大家观察它怎么自己完成。"

**命令**（若实时演示）：
```
分析 parse.py 和 sample.log，找出 parse.py 的 bug 并修复，修复后运行 python parse.py sample.log 验证输出为 6 条
```

**回放讲解**（配合 session 日志，讲师边滚动边讲解）：

| 时间点 | Agent 行为 | 讲师点评 |
|--------|-----------|---------|
| 00:00 | Agent 收到一句话任务 | "注意——我没有告诉它先读哪个文件。它需要自己做决定。" |
| 00:05 | Agent 选择先读 sample.log | "它先看数据。这是合理的——先理解问题域。" |
| 00:10 | Agent 读 parse.py 并对比 | "它发现 6 行日志 vs 4 条输出，不匹配。它自己做了对比。" |
| 00:20 | Agent 定位到第 9 行正则 | "它找到了具体的代码行——跟我们手动分析的结果一样" |
| 00:25 | Agent 修改代码 | "它用编辑工具改了正则" |
| 00:30 | Agent 运行验证命令 | "自动验证——就像你写完代码下意识跑一下测试" |
| 00:35 | Agent 报告结果 | "修复完成，输出 6 条，匹配日志行数" |

**关键展示点**：
- 全程一句话驱动，没有分步指导
- Agent 自动完成了之前手动 ReAct 演示中需要 4 轮交互才能完成的事
- 回顾前面学的基础技巧——Agent 内部已经在用它们了

**备用方案**：
- **首选**：使用预先保存的 session 日志回放
- **备选**：若实时执行足够快（< 2 分钟），直接实时演示
- **兜底**：使用预先录制的 asciicast/截图

**衔出过渡**：
"刚才的演示看起来很流畅，但说实话，Agent 不是每次都这么顺利。就像自动驾驶——高速上很稳，但遇到施工路段还是需要司机接管。我们来聊聊 Agent 的边界在哪里。"

---

### 步骤 4：Agent 的局限与边界

**前情过渡**：
"刚才的演示看起来很流畅，但说实话，Agent 不是每次都这么顺利。"

**讲师话术**：
"Agent 有三大边界，每个使用 Agent 的开发者都应该知道。"

**边界一：任务越模糊，Agent 越容易跑偏**

"刚才我们的任务是明确的：'分析 parse.py 找 bug 并修复'。如果你说'帮我改进这个项目'——Agent 不知道该做什么，可能乱改代码。"

**边界二：Agent 的修改需要人工 review**

"Agent 的代码和人类一样可能有 bug。它修好了 parse.py 的正则，但也许它的修复引入了新问题。永远 review Agent 的改动，就像 review 同事的 PR。"

**边界三：在隔离环境中运行 Agent**

"Agent 有修改文件的能力。在生产服务器上直接跑 Agent？不建议。用 git worktree、Docker 容器或 VM 来隔离 Agent 的操作环境。"

展示 Mermaid 决策树（取自讲义）：

"记住这个决策流程：任务明确 + 范围可控 + 结果可验证 → 用 Agent；否则 → 自己动手或监督执行。"

**关键展示点**：
- 不是所有任务都适合 Agent
- 三个条件：目标明确、范围受控、可验证
- Agent 是工具，不是替代品

**衔出过渡**：
"我们从'怎么写好一句话'开始，一路走到了'怎么让 AI 自己完成整个任务'。让我们退一步，回顾一下这一小时我们学到了什么，更重要的是——明天上班你能立刻用上什么。"
